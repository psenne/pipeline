{
    "rules": {
        ".read": "auth !== null && auth.provider == 'google'",
        ".write": "auth !== null && auth.provider == 'google'",
        "candidates": {
            ".indexOn": ["create_date", "modified_date"]
        },
        "auditing": {
            ".indexOn": ["eventdate"]
        }
    }
}
